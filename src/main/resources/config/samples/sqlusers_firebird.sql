CREATE TABLE USERS (
    LOGIN VARCHAR(50) NOT NULL,
    PASSWORD VARCHAR(150),
    OBJECTSID VARCHAR(50),
    NAME VARCHAR(50),
    EMAIL VARCHAR(50),
    PHONE VARCHAR(50),
    ORGANIZATION VARCHAR(50),
    FAX VARCHAR(50),
    IS_BLOCKED BOOLEAN
);

ALTER TABLE USERS ADD CONSTRAINT PRIMARY_KEY PRIMARY KEY (LOGIN);

